# 全量重算前复核结论与现行边界

> 当前状态：2026-05-08 复核版。本文保留“全量重算前”的历史判断。当前读者入口请优先读 `README.md` 和 `reports/88_*`；v2 无实测边界请读 `reports/87_*` 和 `reports/84_*`；v1 full-grid 与 Tsuyama 计算侧证据链仍见 `reports/48_*`、`reports/49_*`、`reports/50_*`。

## 最终判断

2026-04-24 的结论是：若目标只是恢复 governed case-row engineering library，除了全量重算，没有其他已知可修正代码项需要先处理。

已完成：

- 旧文件43路线图中的可落地修正项已完成。
- `schema 1.24` 已覆盖 reference / collection / detector / calibration / readout / threshold / count / uncertainty / interface / thermal-POD 等 governed provenance。
- 旧文件43 P0-P6 hard gate 已完成：reference active-source claim、signed phase/polarity、joint overlap semantics、baseline-normalized absolute route blocker、sampled-carrier readout validity、material DB coverage / `k_m` alias、package smoke regression。
- 旧 `fine_full_range_biomimetic_exosome_10000e_*` 全量结果已删除，避免被误当 current truth；当前正式目标前缀为 `ev_design_full_range_biomimetic_exosome_with_anchors_10000e_*`。
- lint、typecheck、security scan、pytest 已通过；依赖扫描仅剩已知 `pip 26.0.1 / CVE-2026-3219` 无修复版本项。

2026-04-25 补充：

- 旧文件43完成态已归档到 `archive/tsuyama/59_Tsuyama对齐主链升级路线图与修改框架_2026-04-24完成态.md`。
- 根目录文件43现在冻结为 v5 EV/NODI 设计决策层路线图，目标已从单纯 chip ranking 扩展为 chip + assay-control + wavelength/objective/exposure claim 验证矩阵。
- 如果下一次全量重算只用于 governed case-row engineering ranking，2026-04-24 的“可以重算”结论仍成立。
- 如果下一次全量重算要输出 relative/proxy/diagnostic EV design decision library，文件43 v5 的 P0-hard gates、minimum required schema、P0-soft skeleton/blocker、smoke-critical dashboard/export 路径和 P1 diagnostic/scaffold lane 已完成到可重算状态。

2026-04-26 补充：

- P2.1 BFP ROI mask 已完成 calibration-ready 接口、manifest guard、synthetic guard 和 dashboard 暴露；只有 measured/experimental role、字段完整、manifest kind 匹配的 mask 才能把 `bfp_roi_mask_source` 置为 `calibrated_mask`。
- 代码复核收紧了 manifest kind guard；错 lane manifest 不再显示为 `valid_minimal_manifest`，也不能通过 BFP ROI mask gate。
- 在没有实测 blank / standard particle / BFP ROI / lock-in transfer / detector-unit 数据时，P2/P3 保持 data-blocked，不再作为当前全量重算前置代码修正项推进。
- 全量重算性能路径已补充不改变结果的优化：`standard` artifact profile 已成为 precompute 默认值并减少最终导出 I/O，`TrajectoryContext` 复用 event-invariant 轨迹常量，sweep worker-local cache 复用 case-invariant optics/reference/operator payload，summary-only 路径跳过未消费诊断并保持 RNG 顺序。
- 正式 precompute 默认事件引擎已按 16C/32T 全量机器基准切回 `vectorized_event_engine="off"`，并保留 `event_block_size=32`、`event_block_rng_order="event_loop_order"`。10000-event / 64-case / 28-worker 对照中，scalar/off 为 `74.062 s`，`event_block_v3_b32` 为 `131.048 s`，`event_block_v3_b16` 为 `107.604 s`；`block_lane_order` 只有小幅速度收益且有统计和 gate 漂移，因此不进入默认主线。

2026-04-28 补充：

- `design_postprocess.py` 和 `design_claim_governance.py` 已把 relative/within-lambda recommendation 与 absolute/global green claim 分开；`detector_operator_gate_passed=False` 不再直接硬杀 relative ranking，而是输出 detector caution / detector-resolved relative eligibility。
- `parameter_sweep.py` 的 `analytic_lockin_surrogate` 已有 bandpass/envelope 数值分支；仍不是 measured transfer function。
- `event_quality_control.py` 已输出 stricter soft/hard QC gate 与原因字段。
- `dashboard/config.py` 已新增 `ev_design_biomimetic_ensemble_with_anchors`，用于 EV optical-model uncertainty；nominal anchor 库仍是 `full_range_biomimetic_exosome_with_anchors`。
- `reference_field.py` 已让 measured/calibrated `bfp_roi_mask_path` 进入 `tsuyama_bfp_integrated` ROI 积分，并标记为 `calibrated_bfp_roi_mask_projected_1d`；synthetic/template mask 仍不改变数值。
- `dashboard/precompute.py` 的 `diagnostics_long` 和 schema inventory 已导出完整 `DESIGN_CLAIM_GOVERNANCE_FIELDS`。

2026-05-01 补充：

- `parameter_sweep.py` 已输出 selected detector-mode 条件诊断，同时保留 all-crossing `detection_rate` 作为主评分口径。
- selected annulus 默认使用初始位置 edge-norm `0.5-0.8`，用于解释 Tsuyama selected detector-mode / 有效光学区语义；它不进入 `calibration_score` 或工程 gate，但 EV targeted panel 与全量 size-weighted route analysis 会输出独立 selected-annulus ranking / comparison，供 all-crossing 主口径交叉验证。
- `results/selected_annulus_fullchain_dryrun_20260501_v2/` 已完成 `coarse + quick` 小型全链路试算：`128` precompute rows、`64` EV route rows、`4` prior comparison rows，selected-annulus lens 全部 available。该试算确认 full-grid summary / compact / diagnostics-long 会保存 `selected_detector_mode_annulus_*` 源列，route analysis 可直接消费，不会再退化为缺列 unavailable。
- `tools/tsuyama_detection_rate_calibration.py` 与 `tools/tsuyama_detection_rule_sensitivity.py` 的报告汇总已展示 Au20/Au30/Au40/Au60 全部 selected-candidate / selected-annulus 诊断列。
- `tools/tsuyama_gold_aligned_detection_lane.py` 的 EV targeted panel 代码已导出 selected-mode 诊断列、all-crossing/selected-annulus 双 ranking、comparison availability/status；已有旧 CSV 需要重跑后才会物化这些列。若旧 CSV 被直接用于 comparison，selected-annulus 结果会被置为空/null，并标记 `missing_selected_annulus_columns_rerun_ev_panel_required`。
- `tools/ev_size_weighted_route_analysis.py` 的全量 route analysis 缺 selected-annulus 源列时会保留 primary/all-crossing 输出，但 selected-annulus 检测率与 fraction 输出为 NaN，并跳过 selected ranking。

2026-05-02 补充：

- selected annulus 边界已从纯硬编码提升为 `SimulationConfig.selected_annulus_edge_norm_min/max`，默认仍为 `0.5-0.8`，并校验 `0 <= min < max <= 1`。
- `dashboard.precompute` 的 `meta.json` 现在会写出 `analysis_lanes`，并在 schema inventory route contract 中记录 selected-annulus source、edge-norm 边界、claim level 和 paper alignment target；selected-annulus 的 claim/target 字符串已集中到 `design_claim_governance.py`，当前 paper target 统一为 `tsuyama_2022_nodi_table_s1`。
- `design_claim_governance.py` 现在会校验 `tsuyama_2022_nodi_table_s1` 的 readout metadata：NODI lock-in 约 `3000 Hz`、threshold `5/10 sigma`、minimum peak width 约 `2.5 ms`、magnitude readout、positive pulse detection。selected-annulus claim compatibility 也会比较 target、source、edge-norm min/max 以及工程相对口径的 schema / lane，避免跨口径拼接。
- selected-annulus 条件分母为空、列全 NaN，或 selected rate / fraction 不在同一行成对有效时，conditional detection rate、Wilson LB 和 mean edge norm 输出或保留为 `NaN`；`tools/tsuyama_gold_aligned_detection_lane.py` flatten 后也保留 NaN，下游 route analysis 保留 all-crossing 输出，并把 selected-annulus lens 标为 unavailable，不再把空分母或不成对输入误读成 0% 检出率。
- EV targeted comparison 中如果 selected-annulus 列存在但全为 `NaN`，也必须标记 selected-annulus lens unavailable，并保持 selected ranking 为空；不得把 NaN 回填为 0 后伪造 selected route。
- `joint_fit_score` 已明确为 lower-is-better loss-style penalty；selected-annulus sensitivity decision 会写出 score direction、scenario、target metadata status，以及当前 joint-fit 粒径口径 Au `20/30/40/60 nm`、Ag `40/60 nm`。
- all-crossing 主口径只作为工程 gate / 主 ranking / selected-annulus cross-check，不对齐 paper detection-rate target。selected-annulus paper-audit lane 的当前 2022 NODI surrogate detection-rate targets 是 Au20 `0.30`、Au30 `0.60`、Au40 `0.78`、Au60 `0.92`；2020 POD thermal counting 的近 100% Au20 结论属于不同机制，不能作为本 NODI lane 的目标。
- paper-audit lane 与工程主库 lane 已分层：Phase 2 如需参数反演，只能在 JOINT_CASES × Au `20/30/40/60` + Ag `40/60` 小网格上完成；Phase 3 才能把签发后的 proxy 配置推广到 EV full-grid 工程主库。`tools/tsuyama_selected_annulus_joint_fit.py` 现在会提前拒绝 `in_phase + absolute` 等不满足当前 paper target metadata guard 的 variant。
- Phase 2 值得做，但不阻塞 nominal full-grid。Phase 2 的第一步必须是 target audit：把 detection bands、Au size exponent、Ag/Au ratio、Au30/Au20 SNR ratio 和 classification accuracy 分成 `direct / inferred / operational / diagnostic-only`。当前 selected-annulus joint-fit detection targets 是 surrogate；`tools/tsuyama_detection_rate_calibration.py` 的旧 broad bands 只作 legacy screening，不能和 joint-fit targets 混用。
- Phase 2 hard guardrails 必须先于数值贴合：`analysis_lane=selected_annulus`、annulus `0.5-0.8`、threshold `{5,10}`、lock-in / minimum peak width 在 paper guard 内、`magnitude + positive` readout、annulus fraction >= `0.25`、bounded silver transfer、bounded Au size-response、无 all-crossing paper target、无 global EV default mutation。classification / peak-ratio 在 source audit 前只作为 diagnostic。
- `0.5-0.8` 仍是当前 canonical default；若要在正式全量重算前改变该比例，需要先基于 2022 NODI exact geometry `800/1200 x 550 nm`、`488/532/660 nm` 跑 annulus ratio sensitivity，而不是用 2020 POD `800 x 710 nm` bridge geometry 定 canonical。2026-05-02 已完成 low-event smoke、7-window medium 和 top-window 1000-event focused sensitivity；medium 中 `0.6-0.9` 小幅领先，但 focused run 中 paper-fit candidate 仍以 `0.5-0.8` 最低 penalty，且跨 candidate family 方向不一致，因此不签发新比例。
- 旧 probe/smoke/0p5_0p8 探针结果已从 `results/` 顶层归档到 `archive/tsuyama/probe_pre_20260502/`，避免与后续正式全量结果混淆。

仍未完成但不属于“代码修正项”的内容：

- 真实 blank/reference/operator/standard-particle/blank-trace calibration 数据。
- 高保真 detector-unit、full-wave/interface、thermal POD、colored-noise false-positive、concentration-to-count 定量闭环。
- 若未来要重跑 formal full-grid，应视为复现/扩展任务，不再是当前 blocker。

## 当时可以重算的理由与当前状态

1. 旧结果库已经与 current code / current schema 分离，不能继续作为标准真值。
2. 当前代码已经把 paper-aligned、calibrated、engineering surrogate 三类口径拆开。
3. 无实测数据时，模板与 synthetic fixture 只暴露字段和单位，不会解锁 calibrated claims。
4. 正式 EV full-grid 已完成并生成 current governed engineering / EV design decision library；输出保持 relative/proxy/diagnostic claim level。

## 当前主边界

当前结果允许解释为：

- engineering ranking
- paper-aligned comparison lane
- reference/scattering/detector/readout/count 等分 lane provenance
- calibration-ready schema and templates
- P0/P1 EV design decision blockers, smoke fields, and dashboard visibility

当前结果不允许解释为：

- photon-unit calibrated NODI absolute amplitude
- experimental count-rate prediction
- measured reference phase / absolute polarity claim
- quantitative POD thermal signal
- full-wave near-interface electromagnetic closure
- calibrated SNR / absolute LOD / absolute EV concentration / biological EV specificity

## 复核证据

最终复核运行结果：

- `ruff check .`：通过
- `python -m pyright`：0 errors
- `python -m mypy`：通过
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q`：当前基线 `984 passed`
- USB/exFAT 上生成的 `._*` AppleDouble 元数据文件不属于源码；已从工程范围清理，后续发现后直接删除。
- focused selected-annulus governance/joint-fit/sensitivity：`14 passed`；同一文件集 `ruff` 与 `pyright` 通过。
- selected-annulus annulus-ratio sensitivity：`results/tsuyama_annulus_ratio_sensitivity_smoke_20260502/` 完成真实链路 smoke；`results/tsuyama_annulus_ratio_sensitivity_medium_20260502/` 用 `200` events/case、seeds `42 / 43 / 44`、`8 workers` 扫描 7 个窗口；`results/tsuyama_annulus_ratio_sensitivity_topwindows_1000e_20260502/` 用 `1000` events/case、同三 seed、`8 workers` 复核 top windows。结论是 medium 的 `0.6-0.9` 小幅领先不构成 canonical-change signal；正式全量仍使用 `0.5-0.8`。
- 早期 production-code `bandit` / `pip-audit` 记录保留为当时安全复核证据；当前最新代码基线以 `ruff / pyright / mypy / pytest` 为准。
- `find results -maxdepth 1 -type f -name 'fine_full_range_biomimetic_exosome_10000e_*' -print`：无输出；旧库不会参与 current truth。

性能优化补充验证：

- `ruff check parameter_sweep.py utils.py tests/test_physics_core.py`：通过
- `PYTHONPATH=. .venv/bin/python tests/run_tests.py --workers 8 --skip-app-interactions`：这是 2026-05-02 当时基线；当前正式基线改看 `python -m pytest -q`、`ruff check .`、`pyright` 与 `mypy`。
- `coarse_trial_context_cache_final_*`：`128` cases，`8 workers`，`summary` 与优化前 `coarse_trial_rng_preserve_skip_summary.csv` 按物理 case 排序后完全一致
- 历史性能对照 `codex_event_engine_rng_orders_after_illum_e3000_cases64_repeats2_20260426`：`64` cases，`3000` events/case，`8 workers`，当时的 `event_block_v3_b32` 路径为 `39.1546 s`，scalar 为 `61.0797 s`；该路径检测率/稳定检测率 delta 为 `0`，engineering gate agreement 为 `1.0`。这条历史 8-worker 结论已被 2026-04-27 的 16C/32T / 10000-event 全量机器基准覆盖，当前正式默认是 scalar/off。

## 下一步

下一步按目标分两种：

1. 当前 v1/v2 读者解释以 [reports/88_EV_NODI_v1_v2_consolidated_reader_analysis_with_Tsuyama_comparison.md](./reports/88_EV_NODI_v1_v2_consolidated_reader_analysis_with_Tsuyama_comparison.md) 为准。
2. 当前 v2 只在无实测数据范围内收口；若继续提高可信度，blank / BFP / detector-unit / lock-in / Au pulse trace / size-distribution 实测接入必须另起 post-v2 项目，不应作为 v2 内部下一步。
