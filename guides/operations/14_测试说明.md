# 测试说明

> 文档角色：说明当前测试入口、测试分层和新增测试位置。

## 当前入口

推荐使用：

```bash
zsh tests/run_pytest_suite.sh
```

兼容旧命令：

```bash
python tests/run_tests.py --workers 8
```

`tests/run_tests.py` 现在只是轻量 wrapper，不再复制维护另一套测试实现。

## 测试分层

- `tests/test_physics_core.py`
  核心物理、信号链、读出链、评分与集成回归。
- `tests/test_dashboard_workflow.py`
  dashboard 数据源、页面默认值、工作流联动、AppTest 交互回归。
- `tests/test_ev_selected_annulus_analysis.py`
  selected-annulus EV route / ranking comparison 回归。
- `tests/test_tsuyama_gold_aligned_detection_lane.py`、`tests/test_tsuyama_selected_annulus_joint_fit.py`、`tests/test_tsuyama_2022_classification_lane.py`
  Tsuyama 对齐、joint-fit 与 classification 专项回归。
- `tests/test_tsuyama_paper_target_audit.py`、`tests/test_tsuyama_phase2_acceptance.py`、`tests/test_tsuyama_phase2_parameter_inverse.py`
  Phase 2 target source confidence、acceptance/no-go guardrail 与 family-ladder inverse 专项回归。
- `tests/test_instrument_hardware_feasibility.py`、`tests/test_tsuyama_paper_statistics_sensitivity.py`
  ET-2030 / LI5640 estimated feasibility 与 paper statistics / size-distribution boundary 专项回归。
- `tests/test_event_block_randoms.py`、`tests/test_event_engine_benchmark.py`
  event-block RNG 与 benchmark helper 回归。
- `tests/dashboard_test_helpers.py`
  dashboard 测试夹具和共享断言。

默认脚本会先并行执行非 AppTest 测试，再串行执行真实 Streamlit AppTest：

```bash
python -m pytest tests -m "not app_interactions" -n 8 -q
python -m pytest tests -m "app_interactions" -q
```

## 当前基线

最近一次完整验证：

- full suite: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q` → `984 passed`（`review_bundles/`、`.claude/`、虚拟环境、缓存和生成目录不纳入正式 pytest 收集）
- `ruff check .`、`python -m pyright`、`python -m mypy` 通过
- macOS/USB 生成的 AppleDouble `._*` 元数据文件不属于工程源文件；发现后用 `find . -not -path './.git/*' -not -path './.claude/*' -name '._*' -delete` 清理。
- legacy wrapper: `python tests/run_tests.py --workers 8` 仍保留兼容入口，并覆盖整个 `tests/`；若只复核最新代码状态，优先看上面三条直接命令

当前重点覆盖：

- `run_parameter_sweep` 缺失 `theta_grid_rad` 时前置失败
- case failure 默认不允许静默保存 partial sweep
- `event_block_v3` 在 `event_loop_order` 下与 scalar summary 对照一致
- `block_lane_order` 只作为显式 RNG policy 测试
- 2D light illumination kernel 与 full diagnostics 的信号字段一致
- plug-diffusive trajectory block 保持 scalar RNG 顺序
- configured measured `bfp_roi_mask_path` 会改变 Tsuyama BFP reference ROI 输出，synthetic/template mask 不改变数值
- detector-caution governance 字段进入 `diagnostics_long` 与 schema inventory
- Tsuyama selected detector-mode 条件诊断：summary 输出、calibration/rule 报告汇总、EV targeted panel 诊断列与“不改变 ranking basis”的回归测试
- selected-annulus stale-input 保护：缺列、空分母、列全 NaN，以及 selected rate / fraction 不在同一行成对有效的 comparison / route 输入都会标记 unavailable/null/NaN，不会生成伪 selected ranking
- selected-annulus full-chain dry run：`results/selected_annulus_fullchain_dryrun_20260501_v2/` 验证 `8 workers` precompute 保存、EV route analysis 与 selected-annulus ranking comparison 的端到端字段贯通
- Phase 2 paper-audit：target audit manifest、read-only acceptance report、shadow all-crossing dashboard、A-E family-ladder inverse dry-run、`50 events/case` × seeds `42 / 43 / 44` × `8 workers` smoke、`10000 events/case` × seeds `42 / 43 / 44` × `8 workers` full inverse、Table S1 target-mode audit、seed-median signing、epsilon/severity detection gate、input manifest/hash、Au20 warning-vs-hard-guard acceptance、D2 raw-operator dry-run + `1500 events/case` smoke、D2.1 局部 refphase/collection `2000 events/case` smoke、Phase 2.6 read-only paper-reproduction rescore、size-only reproduction family、3000-event confirmation、Phase 2.7 single global SNR response rescore、Phase 2.8 reviewed/descriptive rescore、Phase 2.9 maximal upper-bound rescore、Phase 2.10 raw Au size-response decomposition、Phase 2.11 single global response-compression rescore、instrument hardware feasibility、paper-statistics sensitivity boundary、score decomposition CSV、reproduction-only no-writeback flags、bounded/maximal fit classification、D2 operator inert diagnostic、classification diagnostic smoke

## 新增测试规则

- 核心物理或信号链行为加到 `tests/test_physics_core.py`。
- dashboard 数据源、页面默认值、跨页状态和交互加到 `tests/test_dashboard_workflow.py`。
- 只有真实 Streamlit UI 交互测试才标记 `@pytest.mark.app_interactions`。
- 新测试必须避免共享固定临时路径、执行顺序依赖和跨测试可变全局状态。
