# EV/NODI P1 Vector Jones Polarization Diagnostic Contract

Date: 2026-05-10

Status:

```text
no-solver rank diagnostic; no vector solver execution
```

This document narrows the second P1 lane into a vector / Jones polarization
diagnostic contract. It remains a surrogate-risk reduction artifact. It does
not change the P0 release conclusion.

## Boundary

The lane may later compare rank evidence from bounded Jones-basis diagnostics
against the P0 scalar and BFP rank evidence. It may not turn polarization
response into calibration, biological specificity, concentration, or detector
prediction evidence.

Required declarations:

```text
calibrated_claim_allowed = false
p0_release_conclusion_changed = false
physical_ceiling_role = surrogate_risk_reduction_only
```

SNR remains blocked. LOD remains blocked. EV concentration remains blocked.
Biological specificity remains blocked. Detector-voltage prediction remains
blocked.

## Inputs

Required P0 sources:

| source | role |
|---|---|
| `results/post_v2_mandatory_audit/top_candidate_mandatory_audit.csv` | route keys, comparison strata, existing rank evidence, and claim blocker columns |
| `results/post_v2_mandatory_audit/top_candidate_pairwise_rank_inversion.csv` | pairwise inversion evidence |
| `results/post_v2_mandatory_audit/bfp_roi_operator_summary.csv` | audited BFP Jacobian provenance |

Future Jones-basis manifests, vector operator exports, or polarization
sensitivity tables require separate authorization. They are not generated by
this contract stage.

Forbidden inputs:

```text
measured biological specificity panels
EV concentration calibration curves
detector-gain calibration
route-specific manual multipliers
raw Jones amplitudes as final gates
```

## Planned Output Schema

Planned path, not generated in this stage:

```text
results/post_v2_physical_ceiling/vector_jones_polarization_diagnostic.csv
```

Required output columns include:

```text
candidate_id
route_key
comparison_stratum
jones_basis_family_id
v1_scalar_rank_percentile_in_stratum
bfp_roi_rank_percentile_in_stratum
jones_rank_percentile_in_stratum
jones_vs_v1_rank_percentile_delta
jones_pairwise_inversion_flag
polarization_surrogate_risk_label
polarization_surrogate_risk_reason_codes
raw_jones_amplitude_proxy_diagnostic_only
raw_magnitude_final_gate_allowed
calibrated_claim_allowed
p0_release_conclusion_changed
physical_ceiling_role
```

Any future output row must keep:

```text
raw_magnitude_final_gate_allowed = false
calibrated_claim_allowed = false
p0_release_conclusion_changed = false
physical_ceiling_role = surrogate_risk_reduction_only
```

## Gate Policy

Allowed future gates:

```text
rank
rank_percentile
pairwise_inversion
```

Raw arbitrary-unit Jones amplitudes may appear only as diagnostic traces and
may not be final gates. Small-stratum changes are secondary notes; the primary
comparison stratum remains `all_ranked_routes`.

## Jacobian Layering

The v1 source library remains:

```text
v1_bfp_to_angle_jacobian_applied = false
```

The post-v2 audit sidecar remains:

```text
audit_bfp_jacobian_applied = true
```

The vector/Jones contract may reference audit-sidecar Jacobian provenance, but
it must not rewrite the v1 source-library Jacobian flag.

## Stop Rule

Stop before implementation if any of the following appears:

```text
vector solver execution
new Jones-basis sweep generation
new polarization case generation
measured-data ingest
route promotion
main-660 redefinition
raw magnitude used as a final gate
v1 event-count expansion
```

The next allowed step is a stub validator or generator that emits only an empty
schema manifest. It is not a vector/Jones implementation.
