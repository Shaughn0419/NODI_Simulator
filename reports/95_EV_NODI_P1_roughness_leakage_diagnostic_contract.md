# EV/NODI P1 Roughness Leakage Diagnostic Contract

Date: 2026-05-10

Status:

```text
no-solver rank diagnostic; no roughness or leakage simulation
```

This document narrows the third P1 lane into a roughness / leakage diagnostic
contract. It remains a surrogate-risk reduction artifact. It does not change
the P0 release conclusion.

## Boundary

The lane may later compare rank evidence from bounded fabrication roughness or
leakage perturbation diagnostics against the P0 scalar and BFP rank evidence.
It may not turn those diagnostics into blank safety, calibration, concentration,
biological specificity, or detector prediction evidence.

Required declarations:

```text
calibrated_claim_allowed = false
p0_release_conclusion_changed = false
physical_ceiling_role = surrogate_risk_reduction_only
```

SNR remains blocked. LOD remains blocked. EV concentration remains blocked.
Biological specificity remains blocked. Detector-voltage prediction remains
blocked. Measured blank safety remains blocked.

## Inputs

Required P0 and governance sources:

| source | role |
|---|---|
| `results/post_v2_mandatory_audit/top_candidate_mandatory_audit.csv` | route geometry, comparison strata, existing rank evidence, and claim blocker columns |
| `results/post_v2_mandatory_audit/top_candidate_pairwise_rank_inversion.csv` | pairwise inversion evidence |
| `configs/realism_v2/r7_2_operator_artifact_gap_register_plan.yaml` | fabrication, BFP alignment, claim-boundary, and stop-gate governance concepts |

Future roughness manifests, leakage perturbation manifests, or wall/BFP
operator exports require separate authorization. They are not generated by this
contract stage.

Forbidden inputs:

```text
measured blank traces
blank false-positive calibration curves
detector-gain calibration
route-specific manual multipliers
raw leakage amplitudes as final gates
```

## Planned Output Schema

Planned path, not generated in this stage:

```text
results/post_v2_physical_ceiling/roughness_leakage_diagnostic.csv
```

Required output columns include:

```text
candidate_id
route_key
comparison_stratum
perturbation_family_id
v1_scalar_rank_percentile_in_stratum
bfp_roi_rank_percentile_in_stratum
roughness_leakage_rank_percentile_in_stratum
roughness_leakage_vs_v1_rank_percentile_delta
roughness_leakage_pairwise_inversion_flag
roughness_leakage_surrogate_risk_label
roughness_leakage_surrogate_risk_reason_codes
raw_leakage_proxy_diagnostic_only
raw_magnitude_final_gate_allowed
measured_blank_safety_claim_allowed
calibrated_claim_allowed
p0_release_conclusion_changed
physical_ceiling_role
```

Any future output row must keep:

```text
raw_magnitude_final_gate_allowed = false
measured_blank_safety_claim_allowed = false
calibrated_claim_allowed = false
p0_release_conclusion_changed = false
physical_ceiling_role = surrogate_risk_reduction_only
```

## Gate Policy

Allowed future gates:

```text
rank
rank_percentile
pairwise_inversion
```

Raw arbitrary-unit leakage proxies may appear only as diagnostic traces and may
not be final gates. Small-stratum changes are secondary notes; the primary
comparison stratum remains `all_ranked_routes`.

## Jacobian Layering

The v1 source library remains:

```text
v1_bfp_to_angle_jacobian_applied = false
```

The post-v2 audit sidecar remains:

```text
audit_bfp_jacobian_applied = true
```

The roughness/leakage contract may reference audit-sidecar Jacobian provenance,
but it must not rewrite the v1 source-library Jacobian flag.

## Stop Rule

Stop before implementation if any of the following appears:

```text
roughness simulation
leakage simulation
new roughness or leakage case generation
measured-data ingest
blank-trace ingest
blank safety claim
route promotion
main-660 redefinition
raw magnitude used as a final gate
v1 event-count expansion
```

The next allowed step is a stub validator or generator that emits only an empty
schema manifest. It is not a roughness/leakage implementation.
