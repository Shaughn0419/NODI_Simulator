# P15 Next Authorization Design

This directory contains P15 next-gate authorization design artifacts.

P15 is design-only. It does not run another solver and does not create new solver output.

The future phrase `authorize sixth bounded solver lane execution` is recorded as required for a later phase only. It has not already been received in P15.

P15 records P12-to-P14 rank instability as trace-only governance with rank delta `[-1, +1, 0]`. It is not route preference and does not authorize route promotion.

Calibration, measured ingest, mesh generation, operator export, full-wave/vector/roughness/transport execution, route promotion, raw final gates, and solver-native raw final gates remain blocked.
